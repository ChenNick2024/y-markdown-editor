import { useStore } from '@renderer/store'

function RightContent(): JSX.Element {
  // const title = useStore((state: any) => state.title)

  return (
    <div className="h-full overflow-hidden bg-pink-200">
      <div>xxx</div>
    </div>
  )
}

export default RightContent
